{
  "name": "test",
  "version": "1.0.0",
  "description": "A new Node.js TypeScript project",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "dev": "nodemon --watch ./src --ext ts --exec ts-node src/app.ts",
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tsc -w"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "typescript": "^4.5.2"
  }
}